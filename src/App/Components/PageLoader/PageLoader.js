import React from 'react';
import './_pageLoader.less';

const PageLoader = ()=>{
  return(
      <div className="pg-loader">
          <div className="loader"/>
      </div>
  )
};

export default PageLoader;